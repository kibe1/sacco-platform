server:
  port: 8085

spring:
  application:
    name: ussd-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
