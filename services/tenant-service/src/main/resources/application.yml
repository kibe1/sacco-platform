server:
  port: 8082

spring:
  application:
    name: tenant-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
